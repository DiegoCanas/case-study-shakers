global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: "projects-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:3000"]
